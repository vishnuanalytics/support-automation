# Optional external connectors (Phase 15 Google Docs, Phase 20 Gmail-OAuth
# provider). Lazily imported — the runtime services import fine without these
# and only a Google endpoint / the Gmail provider needs them. Installed on
# GitHub Actions (gdoc_sync) and in dev; NOT baked into the base image.
google-auth
google-auth-oauthlib
google-api-python-client

# NB pypdf / python-docx (KB file upload) moved into requirements.txt — the
# worker/api genuinely need them for a self-serve KB, not just dev/Actions.
