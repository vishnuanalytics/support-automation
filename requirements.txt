# ingestion (Phase 1)
beautifulsoup4
lxml
trafilatura
fastembed
neo4j

# shared
supabase
python-dotenv
numpy

# interpreter (Phase 2-4)
langgraph
groq
anthropic
simple-salesforce

# Salesforce CDC subscriber (Phase 20l — ingestion/sf_pubsub)
# gRPC + Avro for the Pub/Sub API stream. grpcio-tools is only needed to
# regenerate pubsub_api_pb2*.py from the vendored proto (dev-time, not here).
grpcio
protobuf
fastavro

# Google Docs connector (Phase 15; optional — no-op unless GOOGLE_CLIENT_ID set)
google-auth
google-auth-oauthlib
google-api-python-client

# Slack + GitHub connectors (Phase 16; optional — no-op unless creds set)
requests

# api (Phase 5)
fastapi
uvicorn[standard]

# tests / CI (Phase 9)
pytest
httpx
