# Used only by `npx playwright test` (playwright.config.ts runs the dev
# server with --mode e2e). Fake values on purpose — every /api/** call and
# the Supabase Auth session are mocked in e2e/mocks.ts, so nothing here
# ever reaches a real network. Safe to commit (no real credentials).
VITE_SUPABASE_URL=https://e2e-fixture.supabase.co
VITE_SUPABASE_ANON_KEY=e2e-fixture-anon-key
VITE_API_BASE=
