.env
.env.*
!.env.example
!web/.env.e2e
venv/
__pycache__/
*.pyc
*.log
*:Zone.Identifier
sf_jwt/

# web (Phase 5)
web/node_modules/
web/dist/
web/.env
web/.env.local
web/*.tsbuildinfo

# Playwright (Phase 9 residual, closed 2026-09-05)
web/playwright-report/
web/test-results/
web/.playwright/

# local case exports — never commit (large, contains customer PII)
gunner urbanpiper old cases/

# HubSpot config file
hubspot.config.yml

# HubSpot webhook companion app — a per-tenant deployment artifact (bakes
# in one tenant's real tenant_id + an ephemeral tunnel targetUrl), not
# reusable project source; the generic receiver code lives in api/main.py.
hubspot-webhook-app/
